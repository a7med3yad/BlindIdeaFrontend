/**
 * External Callback Page — /external-callback
 *
 * This is a legacy alias for OAuthCallbackPage.
 * The backend redirects to /external-callback after Google/GitHub login.
 * Both /external-callback and /oauth-callback use the same handler.
 */
export { default } from './OAuthCallbackPage';
